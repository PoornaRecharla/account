const Create = () => {
    return (
        <>
            <div className="a">Create2/</div>
                {/* {true ? 'currently' : 'not'} */}
                {/* <p>
                    {(() => {
                        switch ('red') {
                        case "red":   return <Create />;
                        case "green": return "#00FF00";
                        case "blue":  return "#0000FF";
                        default:      return "#FFFFFF";
                        }
                    })()}
                </p> */}

        </>
    );
}

export default Create;